apiVersion: upstream.pgv2.percona.com/v2
kind: PGAdmin
metadata:
  name: pgadmin
spec:
  dataVolumeClaimSpec:
    accessModes:
    - "ReadWriteOnce"
    resources:
      requests:
        storage: 1Gi
  serverGroups: []
  users: []
