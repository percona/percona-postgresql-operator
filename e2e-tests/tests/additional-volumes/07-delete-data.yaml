apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      run_psql_local \
        '\c myapp \\\ TRUNCATE TABLE myApp' \
        "$(get_psql_uri additional-volumes postgres)"
