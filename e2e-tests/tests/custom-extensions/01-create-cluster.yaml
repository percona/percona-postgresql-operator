apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 240
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      get_cr | kubectl -n "${NAMESPACE}" apply -f -
