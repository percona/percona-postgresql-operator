apiVersion: pgv2.percona.com/v2
kind: PerconaPGCluster
metadata:
  name: major-upgrade-18-to-19
spec:
  extensions:
    image: <image>
    imagePullPolicy: Always
    storage:
      type: s3
      bucket: pg-extensions
      region: eu-central-1
      secret:
        name: aws-s3-secret
