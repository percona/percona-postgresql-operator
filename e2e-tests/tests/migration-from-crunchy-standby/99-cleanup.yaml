apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 300
delete:
  - apiVersion: pgv2.percona.com/v2
    kind: PerconaPGCluster
    metadata:
      name: percona-standby
  - apiVersion: upstream.pgv2.percona.com/v1beta1
    kind: PostgresCluster
    metadata:
      name: percona-standby
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      # Strip finalizers so deletion is not blocked by a missing operator
      kubectl delete postgrescluster crunchy-source -n "${NAMESPACE}" --ignore-not-found || true
      kubectl patch postgrescluster crunchy-source -n "${NAMESPACE}" \
        --type=json -p='[{"op":"remove","path":"/metadata/finalizers"}]' 2>/dev/null || true

      helm uninstall pgo --namespace "${NAMESPACE}" --ignore-not-found || true

      remove_all_finalizers
      check_operator_panic
      destroy_operator
    timeout: 120
