apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      kubectl -n "${NAMESPACE}" rollout restart deployment/pgbouncer-pgbouncer
    timeout: 30
