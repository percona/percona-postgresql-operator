apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions
      kubectl get pg -o yaml -n "${NAMESPACE}" | grep genera

      check_passwords_leak
