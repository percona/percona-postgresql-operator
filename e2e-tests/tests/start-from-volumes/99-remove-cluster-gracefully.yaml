apiVersion: kuttl.dev/v1beta1
kind: TestStep
delete:
- apiVersion: pgv2.percona.com/v2
  kind: PerconaPGCluster
  metadata:
    name: start-from-volumes
- apiVersion: pgv2.percona.com/v2
  kind: PerconaPGCluster
  metadata:
    name: start-from-volumes-clone
- apiVersion: upstream.pgv2.percona.com/v1beta1
  kind: PostgresCluster
  metadata:
    name: start-from-volumes
- apiVersion: upstream.pgv2.percona.com/v1beta1
  kind: PostgresCluster
  metadata:
    name: start-from-volumes-clone
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      pv_name=$(kubectl -n "${NAMESPACE}" get configmap volume-source \
        -o jsonpath='{.data.pv-name}' 2>/dev/null) || true
      if [ -n "${pv_name}" ]; then
        kubectl patch pv "${pv_name}" \
          -p '{"spec":{"persistentVolumeReclaimPolicy":"Delete"}}' 2>/dev/null || true
      fi

      remove_all_finalizers
      check_operator_panic
      destroy_operator
    timeout: 60
