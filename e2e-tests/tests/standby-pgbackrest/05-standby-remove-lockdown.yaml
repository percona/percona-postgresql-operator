apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 60
    script: |-
      set -o errexit
      set -o xtrace

      kubectl delete networkpolicy block-external-egress -n ${NAMESPACE} --wait=true
