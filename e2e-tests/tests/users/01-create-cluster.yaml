apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      get_cr "users" ${RANDOM}| kubectl -n "${NAMESPACE}" apply -f -
    timeout: 10
