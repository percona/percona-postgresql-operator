apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 30
commands:
  - script: |
      kubectl get configmap 10-read-data \
        -n "${NAMESPACE}" -o json \
        | jq -e '.data["data"] == " 100500"'
