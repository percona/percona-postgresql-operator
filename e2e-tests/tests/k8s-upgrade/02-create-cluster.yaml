apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 60
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      get_cr "k8s-upgrade" ${RANDOM} | kubectl -n "${NAMESPACE}" apply -f -
