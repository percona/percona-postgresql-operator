apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 660
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      kubectl wait --for=jsonpath='{.status.state}'=ready --timeout=600s -n "${NAMESPACE}" pg/backup-size-by-type

