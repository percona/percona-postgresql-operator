apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 60
    script: |-
      set -o xtrace

      source ../../functions

      get_cr \
        | yq 'del(.spec.extensions.pg_tde)' \
        | kubectl -n "${NAMESPACE}" apply -f -
